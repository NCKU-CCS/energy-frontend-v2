import React from 'react';
import classnames from 'classnames';

const TimeInfo: React.FC = () => {
  return <div className={classnames('home-time-info')}>Time info.</div>;
};

export default TimeInfo;
