import React from 'react';
import classnames from 'classnames';

const GraphContainer: React.FC = () => {
  return (
    <div className={classnames('home-graph-container')}>Graph container</div>
  );
};

export default GraphContainer;
